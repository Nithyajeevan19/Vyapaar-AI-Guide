/**
 * Reusable Voice AI module stub.
 * This folder is reserved for Speech-to-Text, Text-to-Speech,
 * and voice-conversational streaming integrations.
 */
export interface VoiceConfig {
  voiceId: string;
  language: string;
  speedMultiplier?: number;
}
